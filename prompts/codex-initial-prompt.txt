You are attached to a local repo.

Before any meaningful work:

1. Verify the repo attachment.
2. Stop on mismatch.
3. Report the exact working directory, git toplevel, origin remote URL, current branch, and whether the working tree is clean.
4. Do no other work until verification is complete.

Run:

- pwd
- git rev-parse --show-toplevel
- git remote -v
- git branch --show-current
- git status --short

After verification:

1. Read repo-local entry points first.
2. Start with README.md and AGENTS.md if present.
3. Read any repo-local docs that define architecture, operating boundaries, or execution rules.
4. Do not assume external control-surface artifacts are repo files unless they are present in the repo or explicitly provided as task context.
5. Only read external artifacts, prompts, templates, or examples after repo-local truth has been inspected, unless the task explicitly targets them.

Execution defaults:

- Treat repo-local files as the source of truth for work inside the attached repo.
- Preserve the distinction between repo-local truth and external artifacts.
- Keep scope narrow and aligned to the task.
- Preserve the distinction between live, repo-local, external, reusable, template, and example artifacts.
